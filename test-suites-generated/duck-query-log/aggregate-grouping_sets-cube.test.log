SET default_null_order='nulls_first';;
PRAGMA enable_verification;
create table students (course VARCHAR, type VARCHAR, highest_grade INTEGER);;
insert into students
		(course, type, highest_grade)
	values
		('CS', 'Bachelor', 8),
		('CS', 'Bachelor', 8),
		('CS', 'PhD', 10),
		('Math', 'Masters', NULL),
		('CS', NULL, 7),
		('CS', NULL, 7),
		('Math', NULL, 8);;
select course, count(*) from students group by cube (course) order by 1, 2;;
select course, count(*) from students group by cube (course) order by 1, 2;;
;
select course, count(*) from students group by cube (course) order by 1, 2;;
SELECT course, count_star() FROM students GROUP BY GROUPING SETS ((),(course)) ORDER BY 1, 2;
;
;
;
select course, count(*) from students group by cube (course) order by 1, 2;;
select course, type, count(*) from students group by cube (course, type) order by 1, 2, 3;;
select course, type, count(*) from students group by cube (course, type) order by 1, 2, 3;;
;
select course, type, count(*) from students group by cube (course, type) order by 1, 2, 3;;
SELECT course, "type", count_star() FROM students GROUP BY GROUPING SETS ((),(course),(course, "type"),("type")) ORDER BY 1, 2, 3;
;
;
;
select course, type, count(*) from students group by cube (course, type) order by 1, 2, 3;;
select course, type, count(*) from students group by cube ((course, type)) order by 1, 2, 3;;
select course, type, count(*) from students group by cube ((course, type)) order by 1, 2, 3;;
;
select course, type, count(*) from students group by cube ((course, type)) order by 1, 2, 3;;
SELECT course, "type", count_star() FROM students GROUP BY GROUPING SETS ((),(course, "type")) ORDER BY 1, 2, 3;
;
;
;
select course, type, count(*) from students group by cube ((course, type)) order by 1, 2, 3;;
select course, type, count(*) from students group by cube (course, type, course) order by 1, 2, 3;;
select course, type, count(*) from students group by cube (course, type, course) order by 1, 2, 3;;
;
select course, type, count(*) from students group by cube (course, type, course) order by 1, 2, 3;;
SELECT course, "type", count_star() FROM students GROUP BY GROUPING SETS ((),(course),(course, "type"),(course, "type"),(course),("type"),(course, "type"),(course)) ORDER BY 1, 2, 3;
;
;
;
select course, type, count(*) from students group by cube (course, type, course) order by 1, 2, 3;;
select course, type, highest_grade, count(*) from students group by cube (course, type, highest_grade) order by 1, 2, 3, 4;;
select course, type, highest_grade, count(*) from students group by cube (course, type, highest_grade) order by 1, 2, 3, 4;;
;
select course, type, highest_grade, count(*) from students group by cube (course, type, highest_grade) order by 1, 2, 3, 4;;
SELECT course, "type", highest_grade, count_star() FROM students GROUP BY GROUPING SETS ((),(course),(course, "type"),(course, "type", highest_grade),(course, highest_grade),("type"),("type", highest_grade),(highest_grade)) ORDER BY 1, 2, 3, 4;
;
;
;
select course, type, highest_grade, count(*) from students group by cube (course, type, highest_grade) order by 1, 2, 3, 4;;
select course, type, count(*) from students group by cube (course), cube (type) order by 1, 2, 3;;
select course, type, count(*) from students group by cube (course), cube (type) order by 1, 2, 3;;
;
select course, type, count(*) from students group by cube (course), cube (type) order by 1, 2, 3;;
SELECT course, "type", count_star() FROM students GROUP BY GROUPING SETS ((),("type"),(course),(course, "type")) ORDER BY 1, 2, 3;
;
;
;
select course, type, count(*) from students group by cube (course), cube (type) order by 1, 2, 3;;
select course as crs, type, count(*) from students group by cube (crs), (), type order by 1, 2, 3;;
select course as crs, type, count(*) from students group by cube (crs), (), type order by 1, 2, 3;;
;
select course as crs, type, count(*) from students group by cube (crs), (), type order by 1, 2, 3;;
SELECT course AS crs, "type", count_star() FROM students GROUP BY GROUPING SETS (("type"),(crs, "type")) ORDER BY 1, 2, 3;
;
;
;
select course as crs, type, count(*) from students group by cube (crs), (), type order by 1, 2, 3;;
select course as crs, type as tp, count(*) from students group by grouping sets (cube (crs)), (), tp order by 1, 2, 3;;
select course as crs, type as tp, count(*) from students group by grouping sets (cube (crs)), (), tp order by 1, 2, 3;;
;
select course as crs, type as tp, count(*) from students group by grouping sets (cube (crs)), (), tp order by 1, 2, 3;;
SELECT course AS crs, "type" AS tp, count_star() FROM students GROUP BY GROUPING SETS ((tp),(crs, tp)) ORDER BY 1, 2, 3;
;
;
;
select course as crs, type as tp, count(*) from students group by grouping sets (cube (crs)), (), tp order by 1, 2, 3;;
select course, count(*) from students group by cube (cube (course)) order by 1, 2;;
select course, count(*) from students group by cube (cube (course)) order by 1, 2;;
;
select course, count(*) from students group by cube (cube (course)) order by 1, 2;;
select course, count(*) from students group by cube (cube (course)) order by 1, 2;;
select course, count(*) from students group by cube (grouping_sets (course)) order by 1, 2;;
select course, count(*) from students group by cube (grouping_sets (course)) order by 1, 2;;
;
select course, count(*) from students group by cube (grouping_sets (course)) order by 1, 2;;
select course, count(*) from students group by cube (grouping_sets (course)) order by 1, 2;;
