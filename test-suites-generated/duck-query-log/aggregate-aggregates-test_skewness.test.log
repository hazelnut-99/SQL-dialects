PRAGMA enable_verification;
select skewness();
select skewness();
;
select skewness();
select skewness();
select skewness(NULL);
select skewness(NULL);
;
select skewness(NULL);
SELECT skewness(NULL);
;
;
;
;
select skewness(NULL);
select skewness(1);
select skewness(1);
;
select skewness(1);
SELECT skewness(1);
;
;
;
;
select skewness(1);
select skewness(*);
select skewness(*);
;
select skewness(*);
select skewness(*);
select skewness (10) from range (5);
select skewness (10) from range (5);
;
select skewness (10) from range (5);
SELECT skewness(10) FROM range(5);
;
;
;
;
select skewness (10) from range (5);
select skewness (10) from range (5) where 1 == 0;
select skewness (10) from range (5) where 1 == 0;
;
select skewness (10) from range (5) where 1 == 0;
SELECT skewness(10) FROM range(5) WHERE (1 = 0);
;
;
;
;
select skewness (10) from range (5) where 1 == 0;
select skewness(i) from (values (-2e307), (0), (2e307)) tbl(i);
select skewness(i) from (values (-2e307), (0), (2e307)) tbl(i);
;
select skewness(i) from (values (-2e307), (0), (2e307)) tbl(i);
select skewness(i) from (values (-2e307), (0), (2e307)) tbl(i);
create table aggr(k int, v decimal(10,2), v2 decimal(10, 2));;
insert into aggr values
    (1, 10, null),
    (2, 10, 11),
    (2, 10, 15),
    (2, 10, 18),
    (2, 20, 22),
    (2, 20, 25),
    (2, 25, null),
    (2, 30, 35),
    (2, 30, 40),
    (2, 30, 50),
    (2, 30, 51);;
select skewness(k), skewness(v), skewness(v2) from aggr;
select skewness(k), skewness(v), skewness(v2) from aggr;
;
select skewness(k), skewness(v), skewness(v2) from aggr;
SELECT skewness(k), skewness(v), skewness(v2) FROM aggr;
;
;
;
;
select skewness(k), skewness(v), skewness(v2) from aggr;
select skewness(v2) from aggr group by v ORDER BY ALL;
select skewness(v2) from aggr group by v ORDER BY ALL;
;
select skewness(v2) from aggr group by v ORDER BY ALL;
SELECT skewness(v2) FROM aggr GROUP BY v ORDER BY COLUMNS(*);
;
;
;
;
select skewness(v2) from aggr group by v ORDER BY ALL;
select skewness(v2) over (partition by v)
    from aggr order by v;;
select skewness(v2) over (partition by v)
    from aggr order by v;;
;
select skewness(v2) over (partition by v)
    from aggr order by v;;
SELECT skewness(v2) OVER (PARTITION BY v) FROM aggr ORDER BY v;
;
;
;
;
select skewness(v2) over (partition by v)
    from aggr order by v;;
