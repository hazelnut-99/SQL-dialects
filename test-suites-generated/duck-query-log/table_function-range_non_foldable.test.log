PRAGMA enable_verification;
SELECT COUNT(*)
FROM range(
  current_date,
  current_date + interval '7' days,
  interval '1 day'
);
SELECT COUNT(*)
FROM range(
  current_date,
  current_date + interval '7' days,
  interval '1 day'
);
;
SELECT COUNT(*)
FROM range(
  current_date,
  current_date + interval '7' days,
  interval '1 day'
);
SELECT count_star() FROM range(current_date, (current_date + to_days(CAST(trunc(CAST('7' AS DOUBLE)) AS INTEGER))), CAST('1 day' AS INTERVAL));
;
;
;
;
SELECT COUNT(*)
FROM range(
  current_date,
  current_date + interval '7' days,
  interval '1 day'
);
SELECT to_timestamp(range) as entry
    FROM range(
      epoch(date_trunc('month', current_date))::BIGINT,
      epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
      epoch(interval '1 day')::BIGINT
    );
SELECT to_timestamp(range) as entry
    FROM range(
      epoch(date_trunc('month', current_date))::BIGINT,
      epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
      epoch(interval '1 day')::BIGINT
    );
;
SELECT to_timestamp(range) as entry
    FROM range(
      epoch(date_trunc('month', current_date))::BIGINT,
      epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
      epoch(interval '1 day')::BIGINT
    );
SELECT to_timestamp("range") AS entry FROM range(CAST(epoch(date_trunc('month', current_date)) AS BIGINT), CAST(epoch(((date_trunc('month', current_date) + CAST('1 month' AS INTERVAL)) - CAST('1 day' AS INTERVAL))) AS BIGINT), CAST(epoch(CAST('1 day' AS INTERVAL)) AS BIGINT));
;
;
;
;
SELECT to_timestamp(range) as entry
    FROM range(
      epoch(date_trunc('month', current_date))::BIGINT,
      epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
      epoch(interval '1 day')::BIGINT
    );
SELECT to_timestamp(range) as entry
FROM range(
  epoch(date_trunc('month', current_date))::BIGINT,
  epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
  epoch(interval '1 day')::BIGINT
);
SELECT to_timestamp(range) as entry
FROM range(
  epoch(date_trunc('month', current_date))::BIGINT,
  epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
  epoch(interval '1 day')::BIGINT
);
;
SELECT to_timestamp(range) as entry
FROM range(
  epoch(date_trunc('month', current_date))::BIGINT,
  epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
  epoch(interval '1 day')::BIGINT
);
SELECT to_timestamp("range") AS entry FROM range(CAST(epoch(date_trunc('month', current_date)) AS BIGINT), CAST(epoch(((date_trunc('month', current_date) + CAST('1 month' AS INTERVAL)) - CAST('1 day' AS INTERVAL))) AS BIGINT), CAST(epoch(CAST('1 day' AS INTERVAL)) AS BIGINT));
;
;
;
;
SELECT to_timestamp(range) as entry
FROM range(
  epoch(date_trunc('month', current_date))::BIGINT,
  epoch(date_trunc('month', current_date) + interval '1 month' - interval '1 day')::BIGINT,
  epoch(interval '1 day')::BIGINT
);
