PRAGMA enable_verification;
CREATE TABLE types(i INTEGER, j VARCHAR, k HUGEINT, d DOUBLE, e BLOB);;
INSERT INTO types VALUES 
	(1, 'hello', 12, 0.5, BLOB 'a\x00b\x00c'), 
	(2, 'world', -12, -0.5, BLOB ''), 
	(3, NULL, NULL, NULL, NULL);;
SELECT UNNEST(['i', 'j', 'k', 'd', 'e']) column_names,
       UNNEST(['INTEGER', 'VARCHAR', 'HUGEINT', 'DOUBLE', 'BLOB']) column_types,
       UNNEST([MIN(i)::VARCHAR, MIN(j)::VARCHAR, MIN(k)::VARCHAR, MIN(d)::VARCHAR, MIN(e)::VARCHAR]) min,
	   UNNEST([MAX(i)::VARCHAR, MAX(j)::VARCHAR, MAX(k)::VARCHAR, MAX(d)::VARCHAR, MAX(e)::VARCHAR]) max
FROM (SELECT * FROM types) tbl;
SELECT UNNEST(['i', 'j', 'k', 'd', 'e']) column_names,
       UNNEST(['INTEGER', 'VARCHAR', 'HUGEINT', 'DOUBLE', 'BLOB']) column_types,
       UNNEST([MIN(i)::VARCHAR, MIN(j)::VARCHAR, MIN(k)::VARCHAR, MIN(d)::VARCHAR, MIN(e)::VARCHAR]) min,
	   UNNEST([MAX(i)::VARCHAR, MAX(j)::VARCHAR, MAX(k)::VARCHAR, MAX(d)::VARCHAR, MAX(e)::VARCHAR]) max
FROM (SELECT * FROM types) tbl;
;
SELECT UNNEST(['i', 'j', 'k', 'd', 'e']) column_names,
       UNNEST(['INTEGER', 'VARCHAR', 'HUGEINT', 'DOUBLE', 'BLOB']) column_types,
       UNNEST([MIN(i)::VARCHAR, MIN(j)::VARCHAR, MIN(k)::VARCHAR, MIN(d)::VARCHAR, MIN(e)::VARCHAR]) min,
	   UNNEST([MAX(i)::VARCHAR, MAX(j)::VARCHAR, MAX(k)::VARCHAR, MAX(d)::VARCHAR, MAX(e)::VARCHAR]) max
FROM (SELECT * FROM types) tbl;
SELECT unnest(main.list_value('i', 'j', 'k', 'd', 'e')) AS column_names, unnest(main.list_value('INTEGER', 'VARCHAR', 'HUGEINT', 'DOUBLE', 'BLOB')) AS column_types, unnest(main.list_value(CAST(min(i) AS VARCHAR), CAST(min(j) AS VARCHAR), CAST(min(k) AS VARCHAR), CAST(min(d) AS VARCHAR), CAST(min(e) AS VARCHAR))) AS min, unnest(main.list_value(CAST(max(i) AS VARCHAR), CAST(max(j) AS VARCHAR), CAST(max(k) AS VARCHAR), CAST(max(d) AS VARCHAR), CAST(max(e) AS VARCHAR))) AS max FROM (SELECT * FROM "types") AS tbl;
;
;
;
;
SELECT UNNEST(['i', 'j', 'k', 'd', 'e']) column_names,
       UNNEST(['INTEGER', 'VARCHAR', 'HUGEINT', 'DOUBLE', 'BLOB']) column_types,
       UNNEST([MIN(i)::VARCHAR, MIN(j)::VARCHAR, MIN(k)::VARCHAR, MIN(d)::VARCHAR, MIN(e)::VARCHAR]) min,
	   UNNEST([MAX(i)::VARCHAR, MAX(j)::VARCHAR, MAX(k)::VARCHAR, MAX(d)::VARCHAR, MAX(e)::VARCHAR]) max
FROM (SELECT * FROM types) tbl;
SUMMARIZE types;;
SUMMARIZE types;;
;
SUMMARIZE types;;
SUMMARIZE "types";
;
;
;
;
SUMMARIZE types;;
SUMMARIZE SELECT * FROM types;;
SUMMARIZE SELECT * FROM types;;
;
SUMMARIZE SELECT * FROM types;;
SUMMARIZE (SELECT * FROM "types");
;
;
;
;
SUMMARIZE SELECT * FROM types;;
SUMMARIZE VALUES (1.0),(6754950520);;
SUMMARIZE VALUES (1.0),(6754950520);;
;
SUMMARIZE VALUES (1.0),(6754950520);;
SUMMARIZE (SELECT * FROM (VALUES (1.0), (6754950520)) AS valueslist);
;
;
;
;
SUMMARIZE VALUES (1.0),(6754950520);;
SUMMARIZE SELECT 9223372036854775296;;
SUMMARIZE SELECT 9223372036854775296;;
;
SUMMARIZE SELECT 9223372036854775296;;
SUMMARIZE (SELECT 9223372036854775296);
;
;
;
;
SUMMARIZE SELECT 9223372036854775296;;
summarize select bigint from test_all_types();;
summarize select bigint from test_all_types();;
;
summarize select bigint from test_all_types();;
SUMMARIZE (SELECT "bigint" FROM test_all_types());
;
;
;
;
summarize select bigint from test_all_types();;
summarize select 9223372036854775295;;
summarize select 9223372036854775295;;
;
summarize select 9223372036854775295;;
SUMMARIZE (SELECT 9223372036854775295);
;
;
;
;
summarize select 9223372036854775295;;
